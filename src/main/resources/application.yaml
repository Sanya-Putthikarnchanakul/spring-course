spring:
  application:
    name: jpa
  profiles:
    active: dev
  datasource:
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=MY_DEMO;encrypt=true;trustServerCertificate=true
    username: sa
    password: P@ssw0rd
  jackson:
    time-zone: UTC
  redis:
    host: 127.0.0.1
    port: 6379
#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#        generate_statistics: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics